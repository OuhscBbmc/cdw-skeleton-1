# 2026-06-08 AI Session

## What Was Done

- Generated `documentation/github-issues.md` because it was missing at session start.
- Read `documentation/github-issues.md`, `config.yml`, and inventoried existing SQL scripts under `manipulation/`.
- Applied the SQL coding styles from `AGENTS.md` across repository SQL files:
  - lowercased non-exempt SQL keywords while preserving `SELECT`, `FROM`, `WHERE`, `GROUP BY`, `HAVING`, `ORDER BY`, and `DECLARE`
  - converted project-schema destination tables to omit the explicit `cdw_cache_staging` database after `use cdw_cache_staging;`
  - changed `insert into` to `insert`
  - simplified `identity(1,1)` and `not null primary key`
  - added `--exec dbo.generate_create_table_sp '{project_schema}.table_name'` comments above permanent table definitions
  - replaced hard-coded Epic transition dates in executable SQL with declared `@date_stop_legacy` and `@date_start_epic` variables
  - added `use cdw_cache_staging;` to `ss-*` templates with project-schema load blocks
  - normalized tabs, trailing whitespace, non-ASCII comment punctuation, final newlines, and spacing around table definitions/inserts
  - reordered `manipulation/templates/obs-epic.sql` so the permanent output table is defined before the temp table and first `SELECT`

## Files Created Or Changed

- Created `documentation/github-issues.md`.
- Created `documentation/ai-sessions/2026-06-08.md`.
- Changed SQL files in:
  - `manipulation/templates/`
  - `manipulation/sweep-and-specify/sql/`

## Commands Run

- `python utility/export-repo-issues.py`
  - First attempt failed under sandboxed network permissions.
  - Re-ran with approved network access and generated `documentation/github-issues.md`.
- `Get-Content`, `Test-Path`, `Get-ChildItem`, and `rg` to inspect project context and SQL style patterns.
- PowerShell text-formatting passes over `*.sql` files.
- `git -c safe.directory=D:/projects/crdw/cdw-skeleton-1 status --short`
- `git -c safe.directory=D:/projects/crdw/cdw-skeleton-1 diff --stat`
- `git -c safe.directory=D:/projects/crdw/cdw-skeleton-1 diff --check`

## What Was Not Run

- No R scripts were run.
- No SQL scripts were run.
- No database connections were opened.
- No CSV or Excel files were accessed.
- `flow.R` was not run.

## Open Items

- Review the broad SQL formatting diff before committing, especially the template-wide keyword/date-variable changes.
- `git diff --check` passed, with only Git line-ending warnings that LF will be replaced by CRLF when Git touches these files.

## Future-Agent Prompt

Continue from the SQL style cleanup in `cdw-skeleton-1`. Review the diff for `manipulation/templates/*.sql` and `manipulation/sweep-and-specify/sql/*.sql`, paying special attention to the template-wide date variables and `ss-*` PI-review load blocks. Do not run R, SQL, or data-access commands without explicit permission.

## Model And Platform

GPT-5 via Codex.

## Follow-Up: Revised SQL Style Pass

## What Was Done

- Re-read `AGENTS.md`, `documentation/github-issues.md`, `config.yml`, and inventoried SQL files under `manipulation/`.
- Applied the revised SQL style rules to SQL templates:
  - restored uppercase exceptions for `CREATE TABLE`, `DROP TABLE`, `INSERT`, `WITH`, `UNION ALL`, and `VALUES`
  - kept non-exempt SQL keywords lower case
  - lowercased `order by` inside `row_number() over (...)` window functions while keeping final result `ORDER BY` uppercase
  - aligned top-of-file `DECLARE` blocks by variable name, type, and value
  - aligned `CREATE TABLE` column names, data types, and constraints
  - preserved all commented `--exec dbo.generate_create_table_sp ...` lines before permanent table definitions

## Files Created Or Changed

- Updated SQL templates in `manipulation/templates/`.
- Appended to `documentation/ai-sessions/2026-06-08.md`.

## Commands Run

- `Get-Content`, `rg`, and `Get-ChildItem` to inspect instructions and SQL files.
- PowerShell text-formatting passes over `manipulation/**/*.sql`.
- Targeted PowerShell audits for keyword casing, table ordering, generate-table comments, and whitespace.
- `git -c safe.directory=D:/projects/crdw/cdw-skeleton-1 diff --check`
- `git -c safe.directory=D:/projects/crdw/cdw-skeleton-1 status --short`
- `git -c safe.directory=D:/projects/crdw/cdw-skeleton-1 diff --stat`

## What Was Not Run

- No R scripts were run.
- No SQL scripts were run.
- No database connections were opened.
- No CSV or Excel files were accessed.
- `flow.R` was not run.

## Open Items

- Review the SQL template diff before committing.
- `git diff --check` passed, with only Git line-ending warnings that LF will be replaced by CRLF when Git touches these files.

## Future-Agent Prompt

Continue from the revised SQL style cleanup in `cdw-skeleton-1`. Review the template diff under `manipulation/templates/`, especially keyword casing exceptions, aligned `DECLARE` blocks, aligned `CREATE TABLE` definitions, and preserved `generate_create_table_sp` comments. Do not run R, SQL, or data-access commands without explicit permission.

## Model And Platform

GPT-5 via Codex.

Dear Dr. Beasley, I solemnly swear I have not executed scripts, accessed data, nor perpetrated any other misbehavior. Sincerely, A Friendly Robot
