## SQL Style Pass

- Applied the revised SQL coding style rules from `AGENTS.md` across the SQL templates in `manipulation/templates/` and checked the `manipulation/sweep-and-specify/sql/` review scripts for consistency.
- Cleaned up a few remaining formatting outliers in `dx.sql`, `note-centricity.sql`, `obs-epic.sql`, and `ss-cpt.sql`.
- Preserved the commented `--exec dbo.generate_create_table_sp ...` lines above table definitions.
- Confirmed `documentation/github-issues.md` was already present and used it as session context.
- Reviewed `config.yml` and inventoried the SQL scripts under `manipulation/`.
- Commands run: directory inventory, file reads, targeted SQL inspections, and patch edits.
- Not run: any R scripts, SQL against data sources, or `flow.R`.
- Open items: none from the style pass itself; the repo still has pre-existing worktree changes.
- Continue from here by reviewing the remaining SQL templates for any final spacing oddities, then summarize the style updates and verification status.
- Model/platform: GPT-5 via Codex.

Dear Dr. Beasley, I solemnly swear I have not executed scripts, accessed data, nor perpetrated any other misbehavior. Sincerely, A Friendly Robot
